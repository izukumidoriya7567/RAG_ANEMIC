# RAG_ANEMIC
It has cross-encoders, sparse embeddings, and dense embeddings.

Cross encoders help us to rank the results which we got from performing search on sparse vectors and dense vectors.
It has query along with both the results from sparse embeddings and dense embeddings.

Sparse embeddings-:
Dense 